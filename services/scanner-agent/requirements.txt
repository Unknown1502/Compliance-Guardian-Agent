# Scanner Agent runtime dependencies.
# The shared/ packages are installed via the Dockerfile (pip install ./shared).
#
# No ClamAV client library: the daemon speaks a line protocol and the one
# command needed (INSTREAM) is implemented directly in scanner_agent/clamav.py.
# Adding an unmaintained dependency to the service whose whole job is to be
# trustworthy is a poor trade.
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
google-cloud-firestore>=2.16,<3
google-cloud-bigquery>=3.25,<4
google-cloud-storage>=2.16,<3
# Needed to hand a cleared document on to ingestion. Its absence did not fail
# any test — the dev environment has it via the gateway's requirements — and
# only surfaced as an ImportError inside the deployed container.
google-cloud-tasks>=2.16,<3
pydantic>=2.7,<3
PyYAML>=6.0,<7

# --- Security floors for transitive dependencies (pip-audit, 2026-08-08) ---
# Same reasoning as the other services: a transitive dependency with a known
# CVE ships in the container just the same as a direct one.
cryptography>=50.0.0
pyasn1>=0.6.4
h2>=4.4.1
