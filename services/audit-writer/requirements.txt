# Audit Writer runtime dependencies.
# The shared/ packages are installed via the Dockerfile (pip install ./shared).
#
# Deliberately minimal: this service exists to hold one permission, so the
# smaller its dependency surface, the smaller the thing that permission is
# attached to.
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
google-cloud-bigquery>=3.25,<4
# gcp_clients imports firestore and storage at module level, so importing it
# at all requires both even though this service uses neither.
google-cloud-firestore>=2.16,<3
google-cloud-storage>=2.16,<3
pydantic>=2.7,<3
PyYAML>=6.0,<7

# --- Security floors for transitive dependencies (pip-audit, 2026-08-08) ---
cryptography>=50.0.0
pyasn1>=0.6.4
h2>=4.4.1
